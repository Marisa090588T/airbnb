<div class="container">
  <h1>Add new accommodation</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= simple_form_for [@user, @accommodation] do |f|%>
        <%= f.input :name %>
        <%= f.input :description %>
        <%= f.input :price %>
        <%= f.input :location %>
        <%= f.input :property_type, collection: Accommodation::TYPES %>
        <%= f.input :photo, as: :file %>
        <%# <%= f.input :perks, as: :check_boxes, collection: property_type::PERKS, input_html: { multiple: true }, include_hidden: false %>
        <%# <%= f.input :photo, as: :file %>
        <%= f.button :submit, value: 'Add Accommodation', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>